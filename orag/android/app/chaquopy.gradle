android {
    defaultConfig {
        ndk {
            abiFilters "arm64-v8a"
        }

        python {
            buildPython "D:/Work/8th_Sem/venv/Scripts/python.exe"
            pip {
                install "numpy"
                install "certifi"
                install "huggingface_hub"
                install "pypdf"
            }
        }
    }
}
